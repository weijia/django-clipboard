============
Installation
============

At the command line::

    $ easy_install universal_clipboard

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv universal_clipboard
    $ pip install universal_clipboard
